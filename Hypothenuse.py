import math
side1 = float(input("Enter the size of non hypothenuse side1 : "))
side2 = float(input("Enter the size of non hypothenuse side1 : "))
hypothenuse = math.sqrt(math.pow(side1,2) + math.pow(side2,2))
print(hypothenuse)